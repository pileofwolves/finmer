This folder includes module files that are meant for integration-testing.

Currently, these are module files using legacy file format versions. They may be loaded during CI builds, to verify backwards compatibility; e.g. to ensure that older format versions can still be deserialized.
